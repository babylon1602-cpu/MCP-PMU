# PMU Turf Read-Only MCP

This repository provides a **read-only** Model Context Protocol (MCP) server for turf-related PMU data.

## Entry points
- `main.py`: public launcher
- `server.py`: MCP server implementation

## Run
```bash
python main.py
```

## Install
```bash
pip install -r requirements.txt
```
